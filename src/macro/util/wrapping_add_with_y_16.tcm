# Inline Function: wrapping_add_with_y_16
# in = a: value to add to
# out = a: result of a + Y, wrapped to 16 bits

#!import macro/session_vars.tcm

line a: num;

line a += global VAR_S_CPU_REG_Y;
line a = line a & 0xFFFF;
