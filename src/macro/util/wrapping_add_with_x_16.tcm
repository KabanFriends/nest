# Inline Function: wrapping_add_with_x_16
# in = a: value to add to
# out = a: result of a + X, wrapped to 16 bits

#!import macro/session_vars.tcm

line a: num;

line a += global VAR_S_CPU_REG_X;
line a = line a & 0xFFFF;
