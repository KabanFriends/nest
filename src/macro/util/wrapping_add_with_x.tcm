# Inline Function: wrapping_add_with_x
# in = a: value to add to
# out = a: result of a + X, wrapped to 8 bits

#!import macro/session_vars.tcm

line a: num;

line a += global VAR_S_CPU_REG_X;
line a = line a & 0xFF;
